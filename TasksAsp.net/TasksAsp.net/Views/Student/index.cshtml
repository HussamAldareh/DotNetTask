@model List<Student>

<h2>Students List</h2>

<table>
    <tr>
        <th>Name</th>
        <th>Age</th>
    </tr>

    @foreach (var s in Model)
    {
        <tr>
            <td>@s.Name</td>
            <td>@s.Age</td>
        </tr>
    }
</table>

